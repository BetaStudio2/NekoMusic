# 本地开发代理目标。
# `npm run dev` 时，vite dev server 会把 /api、/version 转发到这里，
# 让前端与接口/音频/封面保持同源，无需后端为浏览器放开 CORS。
#
# 后端默认监听 65535（见 backend/src/main/resources/config.yml）。
# 需要指向其它地址（例如线上 https://music.cnmsb.xin）时，
# 复制本文件为 .env.development.local 覆盖（该文件已 gitignore，不会入库）。
VITE_DEV_PROXY_TARGET=http://localhost:65535
